// MAP

const alunos = ["Bruno", "Joyce", "Gigi", "Scooby"]

const resultado = alunos.map((aluno) => {
    return aluno + " TESTE"
})

//console.log(resultado)
//console.log("--------")
//console.log(alunos)

const numeros = [1, 5, 10]

const valoresDobrados = numeros.map((numero) => {
    return numero * 2
})

console.log(valoresDobrados)